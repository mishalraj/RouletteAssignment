CREATE TABLE user (
  user_id BIGINT AUTO_INCREMENT NOT NULL,
  user_name VARCHAR(255) NOT NULL,
  balance_amount DOUBLE NULL,
  current_casino BIGINT NULL,
  CONSTRAINT pk_user PRIMARY KEY (user_id)
);